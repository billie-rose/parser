const OrderedFieldObject = require('../../src/models/ordered-field-object');
const Field = require('../../src/models/field');

describe(OrderedFieldObject.name, function() {
    describe('#toDelimitedString()', function() {
        it("should return a delimited string of the object's properties", function(done) {
            const fields = [
                new Field('test', 'val', 0);
            ];

            const obj = new OrderedFieldObject()
        });
    });
});
